from django.contrib import admin  # noqa: F401 – required even if no registrations


# SiteSetting admin was removed in v3.5 — CRC gateway mode is now
# determined by the selected UI version (≥ 3.5 → enabled).
